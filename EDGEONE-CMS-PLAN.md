# PetRealm（ptrlm.com）EdgeOne 静态网站与后台方案

## 一、GitHub 是什么

GitHub 是一个用于保存网站源代码和版本记录的在线平台。可以把它理解成“网站文件的云端仓库”。

它可以帮助你：

- 在线保存网站源代码；
- 记录每一次修改，出现问题时可以恢复旧版本；
- 将网站文件自动发送给 EdgeOne Pages；
- 配合内容管理后台修改文字、产品和图片；
- 邀请其他开发人员协助维护网站。

GitHub 是境外服务。在中国大陆有时可以正常访问，有时加载较慢或连接不稳定，因此需要提前考虑日常维护体验。

## 二、推荐的网站结构

### 国际通用方案

`Decap CMS 后台 → GitHub 仓库 → EdgeOne Pages 自动部署 → ptrlm.com`

工作流程：

1. 管理员打开 `https://ptrlm.com/admin/`；
2. 通过 GitHub 账号登录；
3. 在后台修改产品、文字或上传图片；
4. Decap CMS 将修改写入 GitHub 仓库；
5. EdgeOne 检测到 GitHub 文件变化；
6. EdgeOne 自动重新发布静态网站；
7. 数分钟后 `ptrlm.com` 显示新内容。

前台仍然是纯静态网站，没有公开数据库，访问速度和安全性较好。

## 三、中国大陆使用时需要注意

GitHub 在中国大陆并非始终无法使用，但可能出现：

- 网页打开速度慢；
- 登录或 OAuth 授权页面加载失败；
- 图片和脚本加载不稳定；
- 后台保存内容时偶尔提交失败；
- 大文件和视频上传速度较慢。

因此，不建议将大视频放入 Decap CMS。首屏视频可以直接上传到腾讯云 COS，再通过静态网站引用视频地址。

如果后台主要由中国大陆人员使用，GitHub + Decap CMS 可以搭建，但日常使用体验存在不确定性。

## 四、OAuth 桥接是什么

OAuth 桥接服务用于安全地连接 Decap CMS 和 GitHub。

它的作用是：

1. 管理员点击“使用 GitHub 登录”；
2. GitHub 确认管理员身份；
3. OAuth 桥接服务取得短期授权；
4. Decap CMS 获得修改指定网站仓库的权限。

不能把 GitHub 密钥直接写入网站 HTML 或 JavaScript，否则任何访客都可能看到密钥。

### 复杂程度

对普通网站管理员来说，初次配置属于中等难度；配置完成后，日常操作并不复杂。管理员只需要打开后台、登录、修改并保存。

初次搭建需要：

- 创建 GitHub OAuth App；
- 设置授权回调地址；
- 部署一个小型 OAuth 服务；
- 配置 GitHub 仓库名称和分支；
- 设置 EdgeOne 自动部署；
- 将 `ptrlm.com` 绑定到 EdgeOne；
- 配置管理员权限。

OAuth 服务可以放在支持边缘函数的平台，但在中国大陆的访问稳定性需要测试。

## 五、针对 PetRealm 的实际建议

### 方案 A：GitHub + Decap CMS + EdgeOne

适合：需要完整后台、自动部署，并且管理员能够比较稳定地访问 GitHub。

优点：

- 国际上使用广泛；
- 网站版本可以恢复；
- 前台保持静态；
- 后台可以管理产品和图片；
- EdgeOne 可以自动部署。

缺点：

- 国内访问 GitHub 可能不稳定；
- 需要 OAuth 桥接；
- 初次配置步骤较多。

### 方案 B：EdgeOne 静态网站 + 腾讯云 COS 图片管理 + 人工发布

适合：优先考虑国内操作稳定和简单，网站内容修改频率不高。

工作方式：

- 网站部署在 EdgeOne；
- 图片和视频放在腾讯云 COS；
- 在 COS 后台直接上传或替换图片；
- 文字或产品结构修改后，重新上传网站 ZIP。

优点：

- 中国大陆操作更稳定；
- 不需要 GitHub OAuth；
- 图片和视频上传方便；
- 技术复杂度低。

缺点：

- 不能直接在统一后台新增完整产品卡片；
- 修改文字仍需更新网站文件；
- 没有完善的版本管理。

### 推荐结论

如果你不熟悉 GitHub，并且后台主要在中国大陆使用，建议第一阶段使用“EdgeOne + 腾讯云 COS + 人工发布”。网站稳定上线后，再增加 GitHub + Decap CMS。

如果你确实希望马上拥有可以新增产品、编辑六语文案和上传图片的 `/admin` 后台，可以继续使用方案 A，但需要接受 GitHub 偶尔访问不稳定的问题。

## 六、EdgeOne 上线操作步骤

1. 登录腾讯云控制台；
2. 开通 EdgeOne；
3. 进入 EdgeOne Pages；
4. 创建新的静态网站项目；
5. 初次可以选择直接上传 PetRealm 网站 ZIP；
6. 将网站入口设置为 `index.html`；
7. 在 EdgeOne 中添加自定义域名 `ptrlm.com`；
8. 按 EdgeOne 提示修改域名 DNS 解析；
9. 等待 HTTPS 证书自动签发；
10. 访问 `https://ptrlm.com` 检查网站；
11. 填写 Getform/Forminit 接口并测试询盘；
12. 后续再决定是否接入 GitHub 自动部署和 CMS。

## 七、后台需要管理的内容

- 狗狗服装；
- 耐咬狗狗玩具；
- 猫咪用品；
- 六种语言产品名称与描述；
- 材质、MOQ、Logo 和包装信息；
- 产品主图和详情图；
- 首页分类图片；
- OEM 服务内容；
- About 页面内容；
- 企业邮箱、WhatsApp 和地址；
- SEO 标题与描述。

## 八、网站数据查看

- EdgeOne 控制台：请求量、流量、缓存和安全数据；
- Microsoft Clarity 或 Google Analytics：访客国家、设备、页面和用户行为；
- Getform/Forminit：询盘内容和客户上传的设计附件。

考虑中国大陆操作便利性，可以优先使用 EdgeOne 自带统计，并根据海外客户分析需求增加 Google Analytics。

## 九、安全建议

- GitHub 账号开启双重验证；
- 后台仅允许指定管理员登录；
- 不要把 GitHub Token 写进网页代码；
- 产品原图和视频在本地及 COS 各保留一份备份；
- 域名、EdgeOne、企业邮箱和 GitHub 使用不同的高强度密码；
- 定期导出询盘数据。
